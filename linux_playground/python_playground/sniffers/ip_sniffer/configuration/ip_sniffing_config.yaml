device: "eth0"  # or wlan0, en0, etc. depending on your system

targets:
  - ip: "192.168.1.1"
    label: "Router"
  - ip: "10.113.241.193"
    label: "My PC"
  - ip: "8.8.8.8"
    label: "Google DNS"

filter:
  protocols:
    - TCP
    - UDP
  ports:
    - 80
    - 443
    - 53

options:
  log_to_file: true
  output_file: "sniffed_packets.log"
  max_packets: 100
